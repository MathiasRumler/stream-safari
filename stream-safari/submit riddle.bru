meta {
  name: submit riddle
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/api/stream/submit
  body: json
  auth: inherit
}

body:json {
  {
    "riddleId": "7",
    "pipeline": ".sorted()"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
